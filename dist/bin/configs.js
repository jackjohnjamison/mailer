/*
    It has been hidden from Git localy by commiting an empty file and then using skip work tree
    
    Hide:
        'git update-index --skip-worktree dist\bin\configs.js'

    Unhide:
        'git update-index --no-skip-worktree dist\bin\configs.js'

*/

const configs = {

}